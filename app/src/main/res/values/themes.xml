<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Projeto" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tool_principal</item>
        <item name="colorPrimaryVariant">@color/second_blue</item>
        <item name="colorOnPrimary">@color/orange</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/tool_principal</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BottomNavigationTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/tool_principal</item>
        <item name="android:itemBackground">@color/tool_principal</item>
        <item name="android:textColorSecondary">@color/orange</item>
    </style>

    <style name="FABAddTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/orange</item>
        <item name="android:itemBackground">@color/white</item>
    </style>

    <style name="FABFotoTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/tool_principal</item>
        <item name="android:itemBackground">@color/white</item>
    </style>

    <style name="NavFABTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/tool_principal</item>
        <item name="android:itemBackground">@color/tool_principal</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/tool_principal</item>
        <item name="android:itemBackground">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:iconPreview">@color/white</item>
    </style>

    </resources>